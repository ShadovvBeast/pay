{
  "name": "sb0-pay",
  "version": "0.4.0",
  "description": "SB0 Pay - Comprehensive Payment Processing Platform",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "dev:backend": "bun --filter backend dev",
    "dev:frontend": "bun --filter frontend dev",
    "build": "bun run build:backend && bun run build:frontend",
    "build:backend": "bun --filter backend build",
    "build:frontend": "bun --filter frontend build",
    "start": "bun --filter backend start",
    "start:prod": "NODE_ENV=production bun --filter backend start:prod",
    "test": "bun test",
    "test:backend": "bun --filter backend test",
    "test:frontend": "bun --filter frontend test",
    "lint": "bun run lint:backend && bun run lint:frontend",
    "lint:backend": "bun --filter backend lint",
    "lint:frontend": "bun --filter frontend lint",
    "type-check": "bun run type-check:backend && bun run type-check:frontend",
    "type-check:backend": "bun --filter backend type-check",
    "type-check:frontend": "bun --filter frontend type-check",
    "db:init": "bun scripts/db-cli.ts init",
    "db:migrate": "bun scripts/db-cli.ts migrate",
    "db:status": "bun scripts/db-cli.ts status",
    "db:reset": "bun scripts/db-cli.ts reset",
    "clean": "bun run clean:backend && bun run clean:frontend",
    "clean:backend": "rm -rf backend/dist backend/node_modules",
    "clean:frontend": "rm -rf frontend/dist frontend/node_modules",
    "install:all": "bun install"
  },
  "devDependencies": {
    "@types/node": "^20.8.0",
    "typescript": "^5.2.0"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/your-org/sb0-pay.git"
  },
  "keywords": [
    "payment",
    "processing",
    "api",
    "fintech",
    "allpay",
    "bun",
    "elysia",
    "react"
  ],
  "author": "SB0 Pay Team",
  "license": "UNLICENSED"
}